"""페이지 3: Detail — 사이트 목적 및 내부 로직 설명 (이모지 없이 일정한 폰트 크기로 구성)"""

from __future__ import annotations

import streamlit as st

from config import (
    AUTOREFRESH_INTERVAL_SEC,
    DEFAULT_STOP_LOSS_LEVELS,
    INITIAL_CAPITAL,
    KILL_SWITCH_THRESHOLD,
    KIS_QUOTE_CACHE_TTL_SEC,
    KIS_RATE_LIMIT_MAX_RETRIES,
    KIS_RATE_LIMIT_RETRY_DELAY_SEC,
    KIS_REQUEST_DELAY_SEC,
    KIS_TOKEN_CACHE_PATH,
    MA_LONG_DAYS,
    MA_SHORT_DAYS,
    MARKET_CLOSE_TIME,
    MARKET_OPEN_TIME,
    SP500_PROXY_ETF,
    VOL_CAUTION_THRESHOLD,
    VOL_DANGER_THRESHOLD,
    VOLATILITY_LOOKBACK_DAYS,
)

_BODY_STYLE = "font-size: 0.95rem; line-height: 1.8;"


def _p(text: str) -> None:
    """제목을 제외한 모든 본문 문단에 동일한 폰트 크기를 적용해 출력한다."""
    st.markdown(f"<div style='{_BODY_STYLE}'>{text}</div>", unsafe_allow_html=True)


def _h(title: str, date: str) -> None:
    """섹션 제목과 해당 내용의 최종 작업일자를 본문과 동일한 폰트 크기로 함께 표시한다."""
    st.markdown(
        f"<div style='{_BODY_STYLE}'><b>{title}</b> "
        f"<span style='opacity:0.55;'>(최종 업데이트: {date})</span></div>",
        unsafe_allow_html=True,
    )


def render():
    st.title("Detail")

    _p(
        "이 사이트는 실제 매매가 아닌 학습 및 시뮬레이션 목적의 미니 퀀트 대시보드입니다. "
        "실시간에 가까운 국내 지수와 종목 시세를 참고하면서, 가상의 자금으로 매수와 매도를 "
        "연습하고 간단한 퀀트 조건식으로 종목을 걸러보는 용도로 만들어졌습니다. "
        "여기서 제공하는 정보는 투자 조언이 아니며, 실제 매매 결정에 그대로 사용해서는 안 됩니다."
    )
    _p(
        "아래 각 항목 제목 옆에는 해당 기능이 마지막으로 수정된 날짜가 함께 표시됩니다. "
        "기능이 계속 추가되고 수정되는 과정을 살펴보거나, 특정 항목이 최근에 바뀌었는지 "
        "확인할 때 참고하시기 바랍니다."
    )

    st.markdown("<br>", unsafe_allow_html=True)
    _h("페이지 구성", "2026-09-09")
    _p(
        "Market Overview 페이지는 코스피와 코스닥 지수, 원/달러 환율, 8대 섹터 대표 종목의 "
        "현재가와 등락률을 보여줍니다. Portfolio & Quant Analysis 페이지는 샘플 조건을 만족하는 "
        "종목을 찾아주는 스크리너와, 가상의 자금으로 매매를 연습하는 페이퍼 트레이딩 기능을 "
        "제공합니다. 이 Detail 페이지는 사이트의 목적과 내부 동작 방식을 정리한 설명 페이지입니다."
    )

    st.markdown("<br>", unsafe_allow_html=True)
    _h("데이터 출처", "2026-09-10")
    _p(
        "코스피와 코스닥 지수의 현재가, 200일 이동평균 계산에 쓰이는 일자별 지수 데이터는 "
        "한국투자증권(KIS) Open API를 통해 가져옵니다. 8대 섹터 대표 종목의 현재가, 등락률, "
        "PER, PBR, RSI 계산에 쓰이는 일자별 종가 데이터 역시 한국투자증권(KIS) Open API에서 "
        "가져옵니다. 원/달러 환율은 한국투자증권 API가 아닌 네이버 금융의 공개 데이터를 사용하며, "
        "1차로 네이버 증권 마켓인덱스 JSON API를 시도하고, 실패하면 네이버 금융 환율 페이지를 "
        "직접 파싱하는 방식으로 한 번 더 시도합니다. 한국거래소의 휴장일 여부 역시 한국투자증권 "
        "Open API의 국내휴장일조회 기능을 사용하며, 이미 설정된 동일한 API 키를 그대로 재사용하므로 "
        "별도의 API 키가 추가로 필요하지 않습니다."
    )

    st.markdown("<br>", unsafe_allow_html=True)
    _h("자동 새로고침 주기", "2026-09-10")
    _p(
        f"화면은 평일(월요일부터 금요일까지) 한국 표준시 기준 {MARKET_OPEN_TIME.strftime('%H:%M')}부터 "
        f"{MARKET_CLOSE_TIME.strftime('%H:%M')}까지의 정규장 운영시간에만 {AUTOREFRESH_INTERVAL_SEC}초 "
        "간격으로 자동으로 새로고침되어 최신 시세를 반영합니다. 주말이거나 정규장 운영시간이 아니면 "
        "자동 새로고침이 멈추고 마지막으로 조회된 시세가 그대로 유지됩니다. 또한 한국거래소 휴장일로 "
        "확인된 날짜에도 같은 방식으로 자동 새로고침이 멈춥니다. 다만 휴장일 조회 자체가 실패하는 "
        "경우에는 휴장일 여부를 알 수 없는 것으로 간주하고, 요일과 시간 조건만으로 정상적으로 "
        "새로고침을 계속 진행합니다."
    )

    st.markdown("<br>", unsafe_allow_html=True)
    _h("API 접근 토큰 캐싱", "2026-09-10")
    _p(
        "한국투자증권 API를 쓰려면 접근 토큰(Access Token)을 먼저 발급받아야 하는데, "
        f"이 토큰은 발급 시각과 함께 프로젝트 폴더의 숨김 파일(`{KIS_TOKEN_CACHE_PATH.name}`)에 "
        "저장되어 재사용됩니다. 저장된 토큰이 아직 유효기간(약 24시간) 안에 있으면 새로 발급받지 "
        "않고 그대로 재사용하고, 만료가 임박했거나 이미 만료된 경우에만 한국투자증권 서버에 새 "
        "토큰을 요청합니다. 이 덕분에 화면을 새로고침하거나 앱을 재시작할 때마다 토큰을 매번 새로 "
        "발급받지 않게 되어, 불필요한 토큰 발급 요청과 그로 인한 알림(카카오톡 등) 폭주, 응답 지연을 "
        "줄여줍니다."
    )

    st.markdown("<br>", unsafe_allow_html=True)
    _h("캐싱 정책", "2026-09-10")
    _p(
        f"동일한 지수나 종목에 대한 반복 조회로 API 호출이 과도하게 발생하지 않도록, 현재가 조회 "
        f"결과는 {KIS_QUOTE_CACHE_TTL_SEC}초 동안 캐시되어 재사용됩니다. 200일 이동평균과 RSI 계산에 "
        "쓰이는 일자별 히스토리 데이터는 더 긴 주기로 캐시됩니다. 휴장일 조회 결과는 같은 날짜에 "
        "대해서는 하루 동안 캐시되어 반복 조회하지 않습니다."
    )

    st.markdown("<br>", unsafe_allow_html=True)
    _h("API 호출 안정화 (초당 거래건수 초과 방지)", "2026-09-09")
    _p(
        f"한국투자증권 API는 초당 호출 건수를 제한하고 있어, 짧은 시간에 여러 종목을 연속으로 "
        f"조회하면 초당 거래건수를 초과했다는 오류(오류코드 EGW00201)가 발생할 수 있습니다. "
        f"이를 방지하기 위해 연속된 API 호출 사이에는 항상 {KIS_REQUEST_DELAY_SEC}초의 지연을 "
        "두고 있습니다."
    )
    _p(
        f"그럼에도 이 오류가 발생하면, 곧바로 실패로 처리하지 않고 {KIS_RATE_LIMIT_RETRY_DELAY_SEC}초 "
        f"동안 대기한 뒤 자동으로 다시 시도합니다. 이 재시도는 최대 {KIS_RATE_LIMIT_MAX_RETRIES}번까지 "
        "이루어지며, 그래도 계속 실패하는 경우에는 조회 실패로 간주되어 직전에 성공적으로 받아온 "
        "마지막 실제 시세를 그대로 유지하는 방식으로 처리됩니다. 이 초당 거래건수 초과 오류가 아닌 "
        "다른 종류의 오류는 재시도 없이 곧바로 실패로 처리됩니다."
    )

    st.markdown("<br>", unsafe_allow_html=True)
    _h("조회 실패 시 처리 방식", "2026-09-09")
    _p(
        "네트워크 오류나 일시적인 응답 지연으로 시세 조회에 실패하는 경우, 화면에는 가짜로 만든 "
        "임시 데이터를 보여주지 않습니다. 대신 직전에 성공적으로 받아온 마지막 실제 시세를 그대로 "
        "표시하면서, 최근 성공 시세를 유지하고 있다는 경고 문구를 함께 보여줍니다. 만약 과거에 "
        "성공적으로 받아온 데이터조차 전혀 없는 경우에는 오류 안내가 표시됩니다."
    )

    st.markdown("<br>", unsafe_allow_html=True)
    _h("페이퍼 트레이딩", "2026-09-09")
    _p(
        f"가상 계좌의 초기 자금은 {INITIAL_CAPITAL:,.0f}원으로 설정되어 있으며, 모든 매수와 매도 "
        "내역은 로컬 데이터베이스에 저장되어 다음 접속에도 유지됩니다. 종목을 매수할 때는 "
        "목표 손절가를 함께 설정할 수 있으며, 선택 가능한 손절 기준은 "
        f"{', '.join(f'{lvl*100:.0f}%' for lvl in DEFAULT_STOP_LOSS_LEVELS)} 중에서 고를 수 있습니다."
    )

    st.markdown("<br>", unsafe_allow_html=True)
    _h("올웨더 포트폴리오 자산군", "2026-09-10")
    _p(
        "Market Overview 페이지 하단에는 8대 섹터 대표주 외에, 레이 달리오의 올웨더(All-Weather) "
        "포트폴리오 개념을 참고해 주식·채권·원자재·현금(방어) 4개 자산군의 국내 상장 ETF를 함께 "
        "볼 수 있는 영역이 있습니다. 하나의 자산군에만 쏠리지 않고 여러 자산군에 나눠 담았을 때 "
        "시장 상황에 관계없이 견디는 힘을 갖추자는 개념입니다. 화면 상단의 전체·주식·채권·원자재· "
        "현금/방어 탭을 눌러 원하는 자산군만 모아볼 수 있으며, 각 종목은 기존 섹터 종목과 동일한 "
        "카드형 레이아웃으로 표시되어 모바일에서도 읽기 편합니다."
    )

    st.markdown("<br>", unsafe_allow_html=True)
    _h("퀀트 스크리너 조건", "2026-09-09")
    _p(
        "스크리너는 다음과 같은 샘플 조건을 만족하는 종목을 찾아 보여줍니다. RSI 지표가 30 이하로 "
        "내려가면 과매도 시그널로 표시합니다. PBR이 1.0보다 낮으면 저PBR 시그널로 표시합니다. "
        "PER이 0보다 크고 10 이하이면 저PER 시그널로 표시합니다. 이 조건들은 하나의 예시일 뿐이며, "
        "특정 종목의 매수나 매도를 추천하는 것이 아닙니다."
    )

    st.markdown("<br>", unsafe_allow_html=True)
    _h("지표 선정 근거", "2026-09-09")
    _p(
        "RSI 30 이하는 기술적 분석 창시자인 와일더(J. W. Wilder)가 정립한 대표적인 과매도 기준입니다. "
        "주가가 단기적으로 과도하게 하락해 반등 가능성이 높은 구간을 뜻합니다."
    )
    _p(
        "PBR 1.0 미만은 벤저민 그레이엄 계열의 가치투자 기준입니다. 기업이 가진 순자산 가치보다 "
        "주가가 낮게 거래되는, 청산가치 이하로 저평가된 상태를 의미합니다."
    )
    _p(
        "PER 10 이하는 국내 증시(코스피) 평균 PER(약 10에서 12배) 대비 기업이 버는 이익에 비해 "
        "주가가 낮게 형성된, 수익가치 저평가 구간을 의미합니다."
    )

    st.markdown("<br>", unsafe_allow_html=True)
    _h("매수 추천 시그널 (전략 엔진)", "2026-09-09")
    _p(
        "매수 추천 시그널은 두 가지 조건을 모두 만족할 때만 발생합니다. 첫 번째는 매크로 국면 "
        "조건으로, 코스피 지수가 200일 이동평균선 위에 위치한 공격 모드일 때 충족됩니다. 두 번째는 "
        "퀀트 조건으로, 해당 종목의 RSI가 30 이하이면서 동시에 PBR이 1.0 미만이고 동시에 PER이 "
        "0보다 크고 10 이하인 경우에 충족됩니다. 두 조건이 동시에 만족되는 종목만 매수 추천 시그널 "
        "카드로 강조되어 표시됩니다. 스크리너 조건을 하나라도 만족하지만 매수 추천 조건까지는 "
        "충족하지 못한 종목은 별도의 관심 목록 카드 형태로 함께 보여줍니다. 각 카드에는 종목명과 "
        "현재가, 시그널 태그, PER·PBR·RSI 수치, 텔레그램 알림 전송 버튼이 담겨 있으며, 좁은 화면에서도 "
        "세로로 늘어지지 않고 한 종목씩 읽기 쉽게 정리되도록 구성되어 있습니다."
    )

    st.markdown("<br>", unsafe_allow_html=True)
    _h("자동 손절 실행", "2026-09-09")
    _p(
        "보유 중인 각 종목은 매수할 때 설정한 목표 손절가 기준과 실시간 평가 수익률을 계속 "
        "비교합니다. 수익률이 설정된 손절 기준(예를 들어 -3% 또는 -5%)에 도달하거나 이보다 더 "
        "하락하면, 해당 포지션은 자동으로 가상 매도 처리되어 현금화되고 매도 이력이 함께 기록됩니다. "
        f"이 확인은 화면이 새로고침될 때마다(정규장 운영시간 중에는 {AUTOREFRESH_INTERVAL_SEC}초 간격으로) "
        "자동으로 다시 이루어집니다. 보유 종목 화면에는 각 종목의 수익률이 손절 기준에 얼마나 "
        "근접했는지를 막대 형태로 함께 보여줍니다."
    )

    st.markdown("<br>", unsafe_allow_html=True)
    _h("리스크 관리 (킬 스위치)", "2026-09-09")
    _p(
        f"보유 현금과 평가 자산을 합한 계좌 전체의 손실률이 {KILL_SWITCH_THRESHOLD*100:.0f}퍼센트에 "
        "도달하거나 이를 넘어서면 계좌 킬 스위치가 자동으로 발동됩니다. 킬 스위치가 발동되면 "
        "어느 페이지에 있든 화면 상단에 경고 안내가 표시되고, 모든 종목에 대한 신규 가상 매수 "
        "버튼이 잠깁니다. 감정적인 추가 매매를 막고 전략을 다시 점검할 수 있도록 하기 위한 장치입니다."
    )

    st.markdown("<br>", unsafe_allow_html=True)
    _h("시장 위험도 경보 (동적 자산배분)", "2026-09-10")
    _p(
        "Portfolio & Quant Analysis 페이지 상단에는 시장 위험도 경보 카드가 있습니다. 옵션 같은 "
        "파생상품을 쓰지 않고도, 인버스 ETF나 현금 비중을 조절하는 것만으로 하락장에 대비하자는 "
        "동적 리스크 관리 개념을 반영했습니다."
    )
    _p(
        f"코스피 지수와, 실제 해외지수 대신 국내에 상장된 '{SP500_PROXY_ETF[0]}' ETF의 원화 기준 "
        f"가격을 S&P500의 대용값으로 사용해 각각 분석합니다. 두 자산 모두 {MA_SHORT_DAYS}일과 "
        f"{MA_LONG_DAYS}일 이동평균선 위치, 그리고 최근 {VOLATILITY_LOOKBACK_DAYS}일간의 일별 등락폭을 "
        "연 단위로 환산한 변동성을 함께 살펴봅니다."
    )
    _p(
        f"현재가가 {MA_SHORT_DAYS}일 이동평균선 아래로 내려갔거나 연환산 변동성이 "
        f"{VOL_CAUTION_THRESHOLD*100:.0f}퍼센트를 넘으면 주의 단계로 분류되어 "
        "\"시장 변동성 증가: 현금 또는 인버스 ETF 비중 20~30% 확보 추천\" 문구가 표시됩니다. "
        f"{MA_SHORT_DAYS}일 이동평균선이 {MA_LONG_DAYS}일 이동평균선 아래로 내려간 데드크로스 상태에서 "
        f"현재가마저 {MA_SHORT_DAYS}일선 아래에 있거나, 변동성이 {VOL_DANGER_THRESHOLD*100:.0f}퍼센트를 "
        "넘으면 위험 단계로 분류되어 \"하락추세 진입: KODEX 200선물인버스2X 비중 확대 권장\" 문구가 "
        "표시됩니다. 두 조건 모두 해당하지 않으면 정상 단계로, 현금이나 인버스 없이 주식 비중 100퍼센트를 "
        "유지해도 좋다는 안내가 표시됩니다. 코스피와 S&P500 프록시 중 더 위험한 쪽의 단계를 전체 경보로 "
        "채택합니다."
    )
    _p(
        "여기서 쓰인 이동평균 기간과 변동성 임계값은 절대적인 정답이 아니라 조정 가능한 참고 기준이며, "
        "실제 매매 판단에 그대로 사용하기보다는 시장 분위기를 가늠하는 보조 지표로 활용하는 것을 "
        "권장합니다."
    )

    st.markdown("<br>", unsafe_allow_html=True)
    _h("텔레그램 알림 연동", "2026-09-09")
    _p(
        "이 대시보드는 실제 텔레그램 봇 API와 연동되어 있습니다. secrets 설정에 텔레그램 봇 "
        "토큰과 채팅 ID가 입력되어 있는 경우, 다음 세 가지 상황에서 실시간 푸시 알림이 자동으로 "
        "전송됩니다. 첫째, 매수 추천 시그널이 새로 포착된 종목에 대해 같은 날 한 번만 전송됩니다. "
        "둘째, 보유 종목이 손절 기준에 도달해 자동으로 매도 처리될 때마다 전송됩니다. 셋째, 계좌 "
        "킬 스위치가 새로 발동되는 시점에 전송되며, 이후 킬 스위치가 해제되었다가 다시 발동되면 "
        "다시 한 번 전송됩니다. 텔레그램 설정이 되어 있지 않거나 일시적인 네트워크 오류가 발생해도 "
        "알림 전송만 조용히 건너뛸 뿐, 대시보드의 다른 기능에는 영향을 주지 않습니다."
    )
    _p(
        "스크리너 화면과 매수 추천 카드에 있는 텔레그램 알림 전송 버튼은 실제 전송 연동이 잘 "
        "되어 있는지 직접 확인해볼 수 있는 테스트용 버튼입니다. 버튼을 누르면 실제로 설정된 "
        "텔레그램 채팅으로 연동 테스트 메시지가 전송됩니다."
    )
    _p(
        "텔레그램 서버 응답이 느려 전송 확인까지 12초를 넘기는 경우에는 실패로 안내하지 "
        "않습니다. 요청 자체는 이미 서버로 전달되었을 가능성이 높기 때문에, 이때는 메시지가 "
        "발송되었으나 응답 확인이 지연되었다는 안내만 노란색 경고로 표시되고 빨간 오류로는 "
        "표시되지 않습니다."
    )

    st.markdown("<br>", unsafe_allow_html=True)
    _h("유의 사항", "2026-09-09")
    _p(
        "이 사이트에서 사용하는 일부 데이터 조회 방식은 공식 문서가 자주 바뀔 수 있는 항목을 "
        "포함하고 있어, 실제 운영 환경에서 예상과 다르게 동작할 가능성이 있습니다. 그런 경우에도 "
        "해당 항목만 개별적으로 조회 실패로 처리되고 사이트 전체가 멈추지 않도록 설계되어 있습니다. "
        "이 사이트에서 제공하는 모든 정보는 참고용이며, 투자 판단과 그 결과에 대한 책임은 "
        "이용자 본인에게 있습니다."
    )
